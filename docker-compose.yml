services:
  frontend:
    build: .
    env_file:
      - .env
    ports:
      - "80:80"
    volumes:
      - app-data:/app
      - ./nginx/logs:/var/log/nginx
    restart: unless-stopped

volumes:
  app-data:
